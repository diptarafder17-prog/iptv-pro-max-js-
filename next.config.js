/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // If you want to export static for GitHub Pages, add:
  // output: 'export',
  // basePath: '/your-repo-name'
};
module.exports = nextConfig;
